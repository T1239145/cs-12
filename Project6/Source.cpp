#include <cmath>
#include <iostream>

using namespace std;

int main()
{
	double =, y, x, endl;
	int()
		cin << x << endl;
	cin << y << endl;
	double larger(x, y)
	{
		double max;
		if (x >= y)
			max = x;
		else
			max = y;
		return max;
	}
}